# understand-abstract_factory_pattern

**CALL NUMBER:** `software_design_patterns.abstract_factory_pattern`
**DEFINITION:** Provides an interface for creating families of related objects without specifying their concrete classes, operating at the level of abstraction.

Invoke this skill to understand `abstract_factory_pattern` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `?`
- **creational_pattern** (d1): A design pattern category focused on object creation mechanisms, abstracting the instantiation process so that code remains independent of the specific classes being created or how they are assembled.

## CONSUMERS (what needs this)
`abstract_factory`

---
*Projected from the `software design patterns` KB (211 concepts / 36 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*