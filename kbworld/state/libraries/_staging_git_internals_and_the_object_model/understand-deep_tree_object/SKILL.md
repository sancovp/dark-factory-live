# understand-deep_tree_object

Subdomain shelf: the top 6 load-bearing concepts of `deep_tree_object` in `git internals and the object model`. Descend to an `understand-{x}` leaf for the full import cone.