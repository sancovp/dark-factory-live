---
name: 0.3.6-understand-essential_claim_directive
description: "[0.3.6] Claim marked as required by the relying party; identity provider must return this claim or return an error sat"
---

# understand-essential_claim_directive

**CALL NUMBER:** `deep_openid_connect.essential_claim_directive`
**DEFINITION:** Claim marked as required by the relying party; identity provider must return this claim or return an error satisfying the acr_values if unable to fulfill.

Invoke this skill to understand `essential_claim_directive` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`claims_request_object`, `claims_verification`

---
*Projected from the `oauth 2 0 and openid connect` KB (174 concepts / 97 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
