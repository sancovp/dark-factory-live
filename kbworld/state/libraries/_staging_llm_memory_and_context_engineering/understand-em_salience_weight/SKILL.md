# understand-em_salience_weight

**CALL NUMBER:** `deep_long_term_memory.em_salience_weight`
**DEFINITION:** Numeric value indicating how memorable or significant an event is; influences consolidation priority and retrieval ranking.

Invoke this skill to understand `em_salience_weight` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `deep_long_term_memory`
- **em_recency_decay** (d1): Algorithm for reducing salience weights of older events over time; models forgetting in episodic memory.

## CONSUMERS (what needs this)
`em_event`, `memory_consolidation`

---
*Projected from the `llm memory and context engineering` KB (438 concepts / 213 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*