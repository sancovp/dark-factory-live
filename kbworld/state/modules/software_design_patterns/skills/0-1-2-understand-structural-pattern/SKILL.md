---
name: 0.1.2-understand-structural_pattern
description: "[0.1.2] A design pattern category focused on composing classes and objects into larger structures, defining relationsh"
---

# understand-structural_pattern

**CALL NUMBER:** `?.structural_pattern`
**DEFINITION:** A design pattern category focused on composing classes and objects into larger structures, defining relationships between entities to build flexible and efficient hierarchies.

Invoke this skill to understand `structural_pattern` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`adapter_pattern`, `bridge_pattern`, `composite_pattern`, `decorator_pattern`, `facade_pattern`, `flyweight_pattern`, `module_pattern`, `proxy_pattern`

---
*Projected from the `software design patterns` KB (211 concepts / 36 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
