# vision: cicd (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-08T00:51:43  DECISION: SECRET-SPRAWL DECISION -- THE REVIEWER-ON-EVERY-REPO STEP MUST BE A GITHUB APP, NOT PER-REPO WORKFLOWS (Isaac 2026-07-08, verbatim concern: am i going to have to put these secrets in every fucking repo?). GROUNDED FACT verified this session: sancovp is a GitHub USER account (gh api users/sancovp .type = User), NOT an org -- so ORG-LEVEL SECRETS (set once, shared to all repos) are NOT available; org secrets would require converting sancovp to an org. THE SPLIT: (1) PUBLISH_TOKEN lives in ONE place only, the monorepo -- the publish workflow runs IN the monorepo and pushes OUT to tier-2 repos; targets never need it. No sprawl for publishing. (2) the reviewer MINIMAX_API_KEY across many repos IS where sprawl would happen: the naive per-repo-workflow design needs the key as a secret in EVERY repo (GITHUB_TOKEN is auto-provided per-repo so that part is free, but the model key is not). On a User account with no org secrets, that is a key copied into every repo -- rejected. THE RIGHT ARCHITECTURE for self-hosting/self-evolution: a GITHUB APP -- register ONE app with ONE private key; install it per-repo with one click (ZERO secret in the target repo); it receives PR-opened webhooks from any installed repo, runs the reviewer CENTRALLY (the model key lives in exactly one place = the app backend), and posts back via a short-lived installation token it mints per-repo from its own key. This is how Dependabot/CodeRabbit avoid the sprawl, AND it is exactly the cross-repo bot identity the self-evolution loop wants -- not extra work, the correct shape. KEY POINT: the App must run the reviewer CENTRALLY (app backend or one workflow), not as a per-repo Action -- a per-repo Action still needs the model key per-repo even WITH an app. So: centralize execution + App identity = one key, one place, forever. This supersedes the implicit per-repo-workflow assumption in the existing cicd-review-on-pr.yml design for the MULTI-REPO case (the single-repo monorepo workflow is fine as-is). Does NOT affect the immediate next step (wire publishing = one secret one repo). No backticks.  tags:[cicd, github-app, secrets, architecture]
