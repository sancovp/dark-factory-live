---
name: 0.1.1-understand-resilience_patterns
description: [0.1.1] A category of fault_tolerance patterns (including circuit_breaker_pattern, retry_pattern, bulkhead_pattern, an
---

# understand-resilience_patterns

**CALL NUMBER:** `?.resilience_patterns`
**DEFINITION:** A category of fault_tolerance patterns (including circuit_breaker_pattern, retry_pattern, bulkhead_pattern, and fallback_pattern) that enable a distributed_system to continue operating gracefully when components fail or degrade.

Invoke this skill to understand `resilience_patterns` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`bulkhead_pattern`, `circuit_breaker_pattern`, `fallback_pattern`, `retry_pattern`

---
*Projected from the `software architecture patterns and styles` KB (211 concepts / 192 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
