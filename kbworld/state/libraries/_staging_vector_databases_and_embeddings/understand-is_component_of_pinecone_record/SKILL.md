# understand-is_component_of_pinecone_record

**CALL NUMBER:** `?.is_component_of_pinecone_record`
**DEFINITION:** The partitive relationship where a dense or sparse vector constitutes one of the mandatory or optional fields of a Pinecone record.

Invoke this skill to understand `is_component_of_pinecone_record` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`pinecone_dense_vector`, `pinecone_sparse_vector`

---
*Projected from the `vector databases and embeddings` KB (262 concepts / 150 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*