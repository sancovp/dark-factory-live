# understand-cream_crumb

**CALL NUMBER:** `deep_baking.cream_crumb`
**DEFINITION:** An off-white crumb shade with subtle warm undertones, arising from moderate fermentation and standard baking producing limited browning compounds.

Invoke this skill to understand `cream_crumb` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`cream_crumb`

---
*Projected from the `sourdough baking` KB (517 concepts / 376 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*