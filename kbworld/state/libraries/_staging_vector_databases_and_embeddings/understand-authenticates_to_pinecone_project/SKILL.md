# understand-authenticates_to_pinecone_project

**CALL NUMBER:** `?.authenticates_to_pinecone_project`
**DEFINITION:** The relationship where a scoped API key grants authenticated access to a specific Pinecone project and its resources.

Invoke this skill to understand `authenticates_to_pinecone_project` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`pinecone_api_key`

---
*Projected from the `vector databases and embeddings` KB (262 concepts / 150 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*