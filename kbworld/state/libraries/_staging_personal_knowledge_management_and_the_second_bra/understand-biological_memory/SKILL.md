# understand-biological_memory

**CALL NUMBER:** `deep_second_brain.biological_memory`
**DEFINITION:** The brain's innate memory system comprising working memory and long-term memory, subject to capacity limits and forgetting curves.

Invoke this skill to understand `biological_memory` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `deep_second_brain`
- **long_term_memory** (d1): The brain's persistent storage system accessible via working memory; characterized by slow retrieval and susceptibility to interference.
- **working_memory** (d1): The limited-capacity cognitive workspace where active thinking occurs; the primary target for cognitive offload via external memory systems.

## CONSUMERS (what needs this)
`external_memory`

---
*Projected from the `personal knowledge management and the second brain` KB (161 concepts / 149 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*