# tree_object

CALL NUMBER: `git_internals_and_the_object_model.tree_object`

Type-2 git object listing directory entries as (mode, name, sha1) tuples; represents a single directory snapshot at a point in time.
