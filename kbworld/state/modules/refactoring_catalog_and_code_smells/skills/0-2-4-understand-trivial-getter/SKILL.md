---
name: 0.2.4-understand-trivial_getter
description: "[0.2.4] A getter that merely returns an internal field with zero transformation or business meaning."
---

# understand-trivial_getter

**CALL NUMBER:** `deep_dispensables.trivial_getter`
**DEFINITION:** A getter that merely returns an internal field with zero transformation or business meaning.

Invoke this skill to understand `trivial_getter` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`accessor_method`, `accessor_mutator_pair`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
