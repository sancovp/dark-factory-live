---
name: 0.1-understand-core
description: "[0.1] the core subdomain of git internals and the object model (1 concepts)"
---

# understand-core

Subdomain shelf: the top 1 load-bearing concepts of `core` in `git internals and the object model`. Descend to an `understand-{x}` leaf for the full import cone.

## Index summary
[0.1] understand-core — a skill index node; opens to 1 branch(es): understand-revision_traversal (skill). Reachable below: understand-revision_traversal.

## Descend — the next layer (1)
Only this layer is loaded now. To descend, use the **Read tool** on a child below — that injects the child's layer. A Bash `cat` reads the bytes but loads nothing; you must use the Read tool:

- 0.1.1-understand-revision_traversal (skill): Read `/home/runner/work/dark-factory-live/dark-factory-live/kbworld/state/libraries/git_internals_and_the_object_model/understand-core/understand-revision_traversal/.claude/skills/0.1.1-understand-revision_traversal/SKILL.md`
