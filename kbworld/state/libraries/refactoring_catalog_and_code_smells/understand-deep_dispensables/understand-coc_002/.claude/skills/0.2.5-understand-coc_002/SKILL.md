---
name: 0.2.5-understand-coc_002
description: [0.2.5] comment_syntax_container: the comment delimiters and surrounding text that physically bind the disabled code e
---

# understand-coc_002

**CALL NUMBER:** `deep_dispensables.coc_002`
**DEFINITION:** comment_syntax_container: the comment delimiters and surrounding text that physically bind the disabled code element, making its source legible but its semantics inert.

Invoke this skill to understand `coc_002` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`coc_001`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
