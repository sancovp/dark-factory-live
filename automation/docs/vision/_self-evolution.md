# vision: cicd (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-07T10:05:56  VISION: SELF-EVOLVING CICD REPO LOOP (Isaac vision, 2026-07-07 -- brainstorm, NOT yet built). Step 1: publish the cicd-reviewer as its OWN github repo (via scalable-publishing), then point the CICD reviewer at itself = self-hosting. Isaac insight: doing this is a FORCING FUNCTION -- once it evolves itself you are forced to resolve the rest of the starsystems outstanding stuff and whittle it down. Step 2: build an automation for evolution-from-github-issue E2E, possibly AS the PR agent: you commit a PR (or issue) whose payload is a PROMPT FILE saying do xyz; an agent receives it, does the work, opens a PR to the repo, ends; then the automation calls the review agent on that PR; loop. The PR IS the work order, the review IS the gate, git IS the audit trail -- no new transport; the PR-agent (MODE=pr) + review-agent (MODE=review) already exist, the new piece is the prompt-file->change evolution agent. THE LOAD-BEARING SAFETY PIECE (Isaac flagged: important this only runs if the author is me or claude/codex/etc agents): a repo that reads a prompt file and modifies itself IS a prompt-injection surface, so the trust boundary is the whole correctness of the system. PROOF OBLIGATION before turn-on: (a) gate on the event actor (github.actor / PR author login) against an allowlist (Isaac github + specific claude/codex bot accounts), confirm the field cannot be spoofed for the chosen trigger; (b) avoid the pull_request_target / untrusted-fork footgun (privileged code on a fork PR leaks secrets/write) -- gate on a trigger unreachable by outside forks; (c) decide whether the gate is who-OPENED-it vs who-WROTE-the-prompt-content (a trusted actor opening someone elses prompt is still an injection path). STATUS: VISION -- none built; composes with agent-rts/GOD-MODE self-population + the scalable-publishing (design-complete, CI-unwired) + the existing cicd-reviewer (run-proof closed). No backticks.  tags:[cicd, self-evolution, autopoiesis, vision]
