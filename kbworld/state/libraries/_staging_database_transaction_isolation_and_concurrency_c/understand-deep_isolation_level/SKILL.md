# understand-deep_isolation_level

Subdomain shelf: the top 6 load-bearing concepts of `deep_isolation_level` in `database transaction isolation and concurrency control`. Descend to an `understand-{x}` leaf for the full import cone.