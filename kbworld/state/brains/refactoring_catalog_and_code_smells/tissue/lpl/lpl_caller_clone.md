# lpl_caller_clone

[deep_bloaters · d1] caller_signature_clone: a pattern where multiple call sites pass the same subset of parameters to a method, signaling that those parameters form a logical unit awaiting formalization as an object
