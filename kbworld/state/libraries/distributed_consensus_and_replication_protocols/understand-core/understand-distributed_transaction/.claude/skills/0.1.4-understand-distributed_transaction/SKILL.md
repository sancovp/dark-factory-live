---
name: 0.1.4-understand-distributed_transaction
description: [0.1.4] A transaction that spans multiple independent nodes or services, requiring coordination across participants to
---

# understand-distributed_transaction

**CALL NUMBER:** `?.distributed_transaction`
**DEFINITION:** A transaction that spans multiple independent nodes or services, requiring coordination across participants to ensure atomicity, typically via two-phase commit, three-phase commit, or compensating transaction patterns.

Invoke this skill to understand `distributed_transaction` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`saga_pattern`

---
*Projected from the `distributed consensus and replication protocols` KB (196 concepts / 187 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
