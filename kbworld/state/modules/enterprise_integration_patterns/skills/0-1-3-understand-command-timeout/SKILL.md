---
name: 0.1.3-understand-command_timeout
description: "[0.1.3] The maximum duration a sender will wait for a command to complete before considering it failed; exceeding this"
---

# understand-command_timeout

**CALL NUMBER:** `?.command_timeout`
**DEFINITION:** The maximum duration a sender will wait for a command to complete before considering it failed; exceeding this triggers timeout handling or retry.

Invoke this skill to understand `command_timeout` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`cmd_007`, `command_message`

---
*Projected from the `enterprise integration patterns` KB (154 concepts / 176 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
