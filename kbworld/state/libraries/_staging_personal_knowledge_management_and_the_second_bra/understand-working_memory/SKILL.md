# understand-working_memory

**CALL NUMBER:** `deep_second_brain.working_memory`
**DEFINITION:** The limited-capacity cognitive workspace where active thinking occurs; the primary target for cognitive offload via external memory systems.

Invoke this skill to understand `working_memory` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`biological_memory`, `memory_boundary`

---
*Projected from the `personal knowledge management and the second brain` KB (161 concepts / 149 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*