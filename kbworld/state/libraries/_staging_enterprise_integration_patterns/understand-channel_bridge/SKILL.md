# understand-channel_bridge

**CALL NUMBER:** `?.channel_bridge`
**DEFINITION:** A connection point that joins two channel segments or a channel to an external system, enabling message flow across the boundary while preserving channel semantics.

Invoke this skill to understand `channel_bridge` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`channel_adapter`

---
*Projected from the `enterprise integration patterns` KB (154 concepts / 176 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*