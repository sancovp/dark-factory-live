---
name: 0.1-understand-core
description: [0.1] the core subdomain of resilience and reliability engineering patterns (1 concepts)
---

# understand-core

Subdomain shelf: the top 1 load-bearing concepts of `core` in `resilience and reliability engineering patterns`. Descend to an `understand-{x}` leaf for the full import cone.

## Index summary
[0.1] understand-core — a skill index node; opens to 1 branch(es): understand-failure_isolation (skill). Reachable below: understand-failure_isolation.

## Descend — the next layer (1)
Only this layer is loaded now. To descend, use the **Read tool** on a child below — that injects the child's layer. A Bash `cat` reads the bytes but loads nothing; you must use the Read tool:

- 0.1.1-understand-failure_isolation (skill): Read `/home/runner/work/dark-factory-live/dark-factory-live/kbworld/state/libraries/resilience_and_reliability_engineering_patterns/understand-core/understand-failure_isolation/.claude/skills/0.1.1-understand-failure_isolation/SKILL.md`
