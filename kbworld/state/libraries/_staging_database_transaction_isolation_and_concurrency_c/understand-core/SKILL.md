# understand-core

Subdomain shelf: the top 6 load-bearing concepts of `core` in `database transaction isolation and concurrency control`. Descend to an `understand-{x}` leaf for the full import cone.