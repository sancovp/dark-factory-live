---
name: 0.1-understand-core
description: [0.1] the core subdomain of llm memory and context engineering (1 concepts)
---

# understand-core

Subdomain shelf: the top 1 load-bearing concepts of `core` in `llm memory and context engineering`. Descend to an `understand-{x}` leaf for the full import cone.

## Index summary
[0.1] understand-core — a skill index node; opens to 1 branch(es): understand-is_a (skill). Reachable below: understand-is_a.

## Descend — the next layer (1)
Only this layer is loaded now. To descend, use the **Read tool** on a child below — that injects the child's layer. A Bash `cat` reads the bytes but loads nothing; you must use the Read tool:

- 0.1.1-understand-is_a (skill): Read `/home/runner/work/dark-factory-live/dark-factory-live/kbworld/state/libraries/llm_memory_and_context_engineering/understand-core/understand-is_a/.claude/skills/0.1.1-understand-is_a/SKILL.md`
