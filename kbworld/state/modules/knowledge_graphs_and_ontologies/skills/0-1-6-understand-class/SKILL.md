---
name: 0.1.6-understand-class
description: "[0.1.6] A concept in an ontology representing a set of entities that share common characteristics, forming the taxonom"
---

# understand-class

**CALL NUMBER:** `knowledge_graphs_and_ontologies.class`
**DEFINITION:** A concept in an ontology representing a set of entities that share common characteristics, forming the taxonomic backbone.

Invoke this skill to understand `class` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `knowledge_graphs_and_ontologies`
- **subclass_of** (d1): A taxonomic relation stating that one class is more specific than (a specialization of) another class.

---
*Projected from the `knowledge graphs and ontologies` KB (177 concepts / 20 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
