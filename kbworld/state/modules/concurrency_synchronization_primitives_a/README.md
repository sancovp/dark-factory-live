# concurrency-synchronization-primitives-a-module — a cultivated, proof-checked knowledge organism

This repository was **grown, not written** — and it is a valid Claude Code **plugin**. It is a neurosymbolic knowledge module about **concurrency synchronization primitives and memory models**, produced end-to-end by a KB factory: every region admitted through a Prolog consistency gate, gaps enumerated by the prover (not hidden), wrongness tracked as open supersede issues rather than silently edited away.

## The numbers

- **277 concepts / 278 relations**, every region gate-admitted
- **43 `understand-*` skills** harvested from the projected library (call-number-addressed — the coordinate is the dependency web)
- **0 certified hyperedges** (the language automaton's KNOWN vocabulary) · **0 certified argument skeletons**

## Use it

Install as a plugin (skills auto-discover) or read `skills/using-concurrency-synchronization-primitives-a/SKILL.md` — the four ways (RAG / agent brain / heaven tools / factory) plus the module's etiquette. The knowledge substrate lives at `skills/using-concurrency-synchronization-primitives-a/data/` as plain JSONL.

## The graph + the OWL

- **Interactive graph**: https://sancovp.github.io/kb-atlas/concurrency_synchronization_primitives_a.html (the KB Atlas — auto-generated pages for every module on the floor)
- **OWL/Turtle**: `skills/using-concurrency-synchronization-primitives-a/data/module.ttl` — 277 typed individuals, 0 reified certificates, 0 typed argument edges. The proof gate underneath stays Prolog; the OWL is a faithful projection.

## Honesty note

The gate proves **coherence, not truth**: content is LLM-cultivated and machine-checked for closure, connectedness, groundedness, and warrant structure. Factual wrongness is expected, priced (by consumer-cone size), and metabolized through supersede issues — never silently retracted. Domain-expert review is what promotes any region to trusted.
