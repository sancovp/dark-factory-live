# understand-primitive_obsession

**CALL NUMBER:** `refactoring_catalog_and_code_smells.primitive_obsession`
**DEFINITION:** Using primitive types where small objects would provide better semantics, type safety, and expressiveness.

Invoke this skill to understand `primitive_obsession` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`behavior_primitive`, `bloaters`, `class_primitive`, `replace_primitive_with_object`, `replace_type_code_with_class`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*