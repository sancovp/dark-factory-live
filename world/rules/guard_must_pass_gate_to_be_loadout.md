# guard_must_pass_gate_to_be_loadout

A skill installed to catch audit_bug_exploit or any class of bug MUST itself survive the gate test. A guard that fails the gate is worse than no guard: it occupies loadout, consumed the cycle, and gives false confidence. Compose proof-of-composition BEFORE declaring any guard loadout-ready.
