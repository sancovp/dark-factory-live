# lpl_invariant_leak

[deep_bloaters · d1] cross_parameter_invariant: a constraint or business rule that logically spans two or more separate parameters but cannot be enforced because the parameters lack a shared container object
