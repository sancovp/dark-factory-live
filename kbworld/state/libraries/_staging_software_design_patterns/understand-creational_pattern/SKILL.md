# understand-creational_pattern

**CALL NUMBER:** `?.creational_pattern`
**DEFINITION:** A design pattern category focused on object creation mechanisms, abstracting the instantiation process so that code remains independent of the specific classes being created or how they are assembled.

Invoke this skill to understand `creational_pattern` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`abstract_factory_pattern`, `builder_pattern`, `dependency_injection_pattern`, `factory_method_pattern`, `object_pool_pattern`, `prototype_pattern`, `service_locator_pattern`, `singleton_pattern`

---
*Projected from the `software design patterns` KB (211 concepts / 36 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*