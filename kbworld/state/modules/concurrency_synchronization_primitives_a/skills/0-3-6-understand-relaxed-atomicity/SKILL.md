---
name: 0.3.6-understand-relaxed_atomicity
description: "[0.3.6] The sole guarantee of memory_order_relaxed: a load or store completes atomically without torn reads or torn wr"
---

# understand-relaxed_atomicity

**CALL NUMBER:** `deep_c11_memory_model.relaxed_atomicity`
**DEFINITION:** The sole guarantee of memory_order_relaxed: a load or store completes atomically without torn reads or torn writes; no other constraints are imposed.

Invoke this skill to understand `relaxed_atomicity` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`relaxed_atomicity`

---
*Projected from the `concurrency synchronization primitives and memory models` KB (211 concepts / 207 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
