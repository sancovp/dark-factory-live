# authorization_code_flow

[oauth_2_0_and_openid_connect · d1] Primary OAuth 2.0 flow for web apps with a server-side component; uses authorization_endpoint then token_endpoint with the returned code.
