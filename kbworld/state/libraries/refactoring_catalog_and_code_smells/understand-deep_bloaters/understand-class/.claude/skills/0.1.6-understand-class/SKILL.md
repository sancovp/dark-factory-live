---
name: 0.1.6-understand-class
description: [0.1.6] Primitive: a type defining fields and methods as a cohesive unit.
---

# understand-class

**CALL NUMBER:** `deep_bloaters.class`
**DEFINITION:** Primitive: a type defining fields and methods as a cohesive unit.

Invoke this skill to understand `class` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`class`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
