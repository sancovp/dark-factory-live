---
name: 0.3.5-understand-program_order_locality
description: "[0.3.5] The property that program order defines a total order of operations only within a single thread; relaxed opera"
---

# understand-program_order_locality

**CALL NUMBER:** `deep_c11_memory_model.program_order_locality`
**DEFINITION:** The property that program order defines a total order of operations only within a single thread; relaxed operations preserve this intra-thread order but nothing beyond it.

Invoke this skill to understand `program_order_locality` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`program_order_locality`

---
*Projected from the `concurrency synchronization primitives and memory models` KB (211 concepts / 207 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
