# understand-deep_type_inference

Subdomain shelf: the top 6 load-bearing concepts of `deep_type_inference` in `type systems for working programmers`. Descend to an `understand-{x}` leaf for the full import cone.