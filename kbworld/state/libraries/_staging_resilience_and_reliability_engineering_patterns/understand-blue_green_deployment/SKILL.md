# understand-blue_green_deployment

**CALL NUMBER:** `resilience_and_reliability_engineering_patterns.blue_green_deployment`
**DEFINITION:** parallel environments with instant traffic switch; enables instant rollback on failure

Invoke this skill to understand `blue_green_deployment` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `resilience_and_reliability_engineering_patterns`
- **zero_downtime_deployment** (d1): deployment technique ensuring continuous availability during version transition

## CONSUMERS (what needs this)
`canary_deployment`

---
*Projected from the `resilience and reliability engineering patterns` KB (118 concepts / 34 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*