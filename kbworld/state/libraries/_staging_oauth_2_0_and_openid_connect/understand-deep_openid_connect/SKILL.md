# understand-deep_openid_connect

Subdomain shelf: the top 6 load-bearing concepts of `deep_openid_connect` in `oauth 2 0 and openid connect`. Descend to an `understand-{x}` leaf for the full import cone.