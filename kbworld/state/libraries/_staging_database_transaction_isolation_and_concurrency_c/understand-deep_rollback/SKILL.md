# understand-deep_rollback

Subdomain shelf: the top 6 load-bearing concepts of `deep_rollback` in `database transaction isolation and concurrency control`. Descend to an `understand-{x}` leaf for the full import cone.