# understand-reads_from_pinecone_index

**CALL NUMBER:** `?.reads_from_pinecone_index`
**DEFINITION:** The data access relationship where fetch or query operations retrieve stored records from a Pinecone index by id or vector similarity.

Invoke this skill to understand `reads_from_pinecone_index` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`pinecone_fetch`, `pinecone_query`

---
*Projected from the `vector databases and embeddings` KB (262 concepts / 150 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*