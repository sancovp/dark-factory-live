---
name: 0.1.1-understand-command_priority
description: "[0.1.1] A metadata field indicating the urgency or precedence level of a command message, used by schedulers and route"
---

# understand-command_priority

**CALL NUMBER:** `?.command_priority`
**DEFINITION:** A metadata field indicating the urgency or precedence level of a command message, used by schedulers and routers to order or filter command processing.

Invoke this skill to understand `command_priority` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`cmd_008`, `command_message`

---
*Projected from the `enterprise integration patterns` KB (154 concepts / 176 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
