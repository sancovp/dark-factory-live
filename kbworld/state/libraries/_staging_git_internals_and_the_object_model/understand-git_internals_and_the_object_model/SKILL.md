# understand-git_internals_and_the_object_model

Subdomain shelf: the top 6 load-bearing concepts of `git_internals_and_the_object_model` in `git internals and the object model`. Descend to an `understand-{x}` leaf for the full import cone.