# embedding_cache

[vector_databases_and_embeddings · d1] A key-value store (Redis, memcached, disk) that persists computed embedding vectors to avoid redundant model inference for identical or duplicate inputs.
