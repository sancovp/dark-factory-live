# understand-fold_count_10

**CALL NUMBER:** `deep_bulk_fermentation.fold_count_10 : sourdough_baking(1)`
**DEFINITION:** Excessive number of folds causing dough exhaustion, overdevelopment, or tearing that degrades crumb_structure and flavor_complexity.

Invoke this skill to understand `fold_count_10` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `sourdough_baking`
- **dough_consistency** (d1): Viscosity and handling quality from soft and extensible to firm and tight

## CONSUMERS (what needs this)
`fold_count_4`

---
*Projected from the `sourdough baking` KB (546 concepts / 365 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*