---
name: 0.1.5-understand-change_frequency_metric
description: "[0.1.5] Measure of how often a field group is modified or extended together."
---

# understand-change_frequency_metric

**CALL NUMBER:** `deep_bloaters.change_frequency_metric`
**DEFINITION:** Measure of how often a field group is modified or extended together.

Invoke this skill to understand `change_frequency_metric` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`change_frequency_metric`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
