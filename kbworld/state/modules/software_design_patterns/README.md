# software-design-patterns-module — a cultivated, proof-checked knowledge organism

This repository was **grown, not written** — and it is a valid Claude Code **plugin**. It is a neurosymbolic knowledge module about **software design patterns**, produced end-to-end by a KB factory: every region admitted through a Prolog consistency gate, gaps enumerated by the prover (not hidden), wrongness tracked as open supersede issues rather than silently edited away.

## The numbers

- **211 concepts / 36 relations**, every region gate-admitted
- **11 `understand-*` skills** harvested from the projected library (call-number-addressed — the coordinate is the dependency web)
- **0 certified hyperedges** (the language automaton's KNOWN vocabulary) · **0 certified argument skeletons**

## Use it

Install as a plugin (skills auto-discover) or read `skills/using-software-design-patterns/SKILL.md` — the four ways (RAG / agent brain / heaven tools / factory) plus the module's etiquette. The knowledge substrate lives at `skills/using-software-design-patterns/data/` as plain JSONL.

## The graph + the OWL

- **Interactive graph**: https://sancovp.github.io/kb-atlas/software_design_patterns.html (the KB Atlas — auto-generated pages for every module on the floor)
- **OWL/Turtle**: `skills/using-software-design-patterns/data/module.ttl` — 211 typed individuals, 0 reified certificates, 0 typed argument edges. The proof gate underneath stays Prolog; the OWL is a faithful projection.

## Honesty note

The gate proves **coherence, not truth**: content is LLM-cultivated and machine-checked for closure, connectedness, groundedness, and warrant structure. Factual wrongness is expected, priced (by consumer-cone size), and metabolized through supersede issues — never silently retracted. Domain-expert review is what promotes any region to trusted.
