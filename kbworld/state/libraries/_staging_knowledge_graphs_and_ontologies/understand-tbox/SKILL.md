# understand-tbox

**CALL NUMBER:** `knowledge_graphs_and_ontologies.tbox`
**DEFINITION:** The terminological box — the schema-level component of a knowledge base defining classes, properties, and their relations.

Invoke this skill to understand `tbox` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`abox`, `axiom`

---
*Projected from the `knowledge graphs and ontologies` KB (177 concepts / 20 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*