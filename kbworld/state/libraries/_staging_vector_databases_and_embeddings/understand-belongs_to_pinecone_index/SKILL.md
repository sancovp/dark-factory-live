# understand-belongs_to_pinecone_index

**CALL NUMBER:** `?.belongs_to_pinecone_index`
**DEFINITION:** The containment relationship indicating a record resides within a specific Pinecone index for storage and retrieval.

Invoke this skill to understand `belongs_to_pinecone_index` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`pinecone_record`

---
*Projected from the `vector databases and embeddings` KB (262 concepts / 150 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*