# memory_persistence

[llm_memory_and_context_engineering · d1] How long memory survives; session memory (within conversation), user memory (across sessions), or permanent memory (until explicitly deleted).
