# understand-anemic_domain_model

**CALL NUMBER:** `deep_dispensables.anemic_domain_model`
**DEFINITION:** A domain model consisting of data classes lacking the business logic that should be encapsulated within the objects themselves.

Invoke this skill to understand `anemic_domain_model` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`anemic_domain_model`, `anemic_state_holder`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*