# commit_object

CALL NUMBER: `git_internals_and_the_object_model.commit_object`

Type-3 git object containing tree SHA, parent SHA(s), author, committer, timestamp, and message; forms the history DAG backbone.
