---
name: 0.1-understand-core
description: "[0.1] the core subdomain of type systems for working programmers (1 concepts)"
---

# understand-core

Subdomain shelf: the top 1 load-bearing concepts of `core` in `type systems for working programmers`. Descend to an `understand-{x}` leaf for the full import cone.

## Index summary
[0.1] understand-core — a skill index node; opens to 1 branch(es): understand-primitive_union (skill). Reachable below: understand-primitive_union.

## Descend — the next layer (1)
Only this layer is loaded now. To descend, use the **Read tool** on a child below — that injects the child's layer. A Bash `cat` reads the bytes but loads nothing; you must use the Read tool:

- 0.1.1-understand-primitive_union (skill): Read `/home/runner/work/dark-factory-live/dark-factory-live/kbworld/state/libraries/type_systems_for_working_programmers/understand-core/understand-primitive_union/.claude/skills/0.1.1-understand-primitive_union/SKILL.md`
