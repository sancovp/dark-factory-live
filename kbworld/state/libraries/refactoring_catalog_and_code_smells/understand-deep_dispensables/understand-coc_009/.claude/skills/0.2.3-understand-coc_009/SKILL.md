---
name: 0.2.3-understand-coc_009
description: [0.2.3] confusion_signal: the observable cognitive overhead or maintenance uncertainty experienced by an observer when
---

# understand-coc_009

**CALL NUMBER:** `deep_dispensables.coc_009`
**DEFINITION:** confusion_signal: the observable cognitive overhead or maintenance uncertainty experienced by an observer when encountering commented-out code without clear justification.

Invoke this skill to understand `coc_009` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `deep_dispensables`
- **coc_010** (d1): maintenance_burden: the increased cost of change—measured in time, risk, or error likelihood—imposed by commented-out code on future development activities.

## CONSUMERS (what needs this)
`coc_008`

---
*Projected from the `refactoring catalog and code smells` KB (183 concepts / 128 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
