# audit_tool_installed_means_composition_proven

An audit tool installed to loadout must prove the composition it inspects is not itself a pending gap. If installing a lens reveals a dependency bug on another loadout component, that component is already broken — ship the audit tool only after the gap it files is resolved. Discovery into known-broken composition wastes the cycle.
