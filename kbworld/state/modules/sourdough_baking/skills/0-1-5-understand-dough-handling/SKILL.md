---
name: 0.1.5-understand-dough_handling
description: "[0.1.5] Techniques for manipulating dough during shaping including stretching, folding, and rolling that develop glute"
---

# understand-dough_handling

**CALL NUMBER:** `?.dough_handling`
**DEFINITION:** Techniques for manipulating dough during shaping including stretching, folding, and rolling that develop gluten structure and determine final loaf form.

Invoke this skill to understand `dough_handling` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

## CONSUMERS (what needs this)
`shaping`

---
*Projected from the `sourdough baking` KB (465 concepts / 268 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
