---
name: 0.1-understand-core
description: "[0.1] the core subdomain of oauth 2 0 and openid connect (1 concepts)"
---

# understand-core

Subdomain shelf: the top 1 load-bearing concepts of `core` in `oauth 2 0 and openid connect`. Descend to an `understand-{x}` leaf for the full import cone.

## Index summary
[0.1] understand-core — a skill index node; opens to 1 branch(es): understand-token_type (skill). Reachable below: understand-token_type.

## Descend — the next layer (1)
Only this layer is loaded now. To descend, use the **Read tool** on a child below — that injects the child's layer. A Bash `cat` reads the bytes but loads nothing; you must use the Read tool:

- 0.1.1-understand-token_type (skill): Read `/home/runner/work/dark-factory-live/dark-factory-live/kbworld/state/libraries/oauth_2_0_and_openid_connect/understand-core/understand-token_type/.claude/skills/0.1.1-understand-token_type/SKILL.md`
