---
name: 0.1.5-understand-blank_node
description: "[0.1.5] An anonymous resource in RDF without a stable IRI, used for existential variables or intermediate blank graph "
---

# understand-blank_node

**CALL NUMBER:** `knowledge_graphs_and_ontologies.blank_node`
**DEFINITION:** An anonymous resource in RDF without a stable IRI, used for existential variables or intermediate blank graph nodes.

Invoke this skill to understand `blank_node` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `knowledge_graphs_and_ontologies`
- **named_individual** (d1): An individual entity that is explicitly named with an IRI, as distinguished from anonymous blank nodes.

---
*Projected from the `knowledge graphs and ontologies` KB (177 concepts / 20 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*

_(leaf — this is an actual skill.)_
